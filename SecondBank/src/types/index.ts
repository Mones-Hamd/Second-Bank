export type INewUser = {
    name: string;
    email: string;
    password: string;
  };
export type IUser = {
    userId: string;
    name: string;
    email: string;
    imageUrl: string;
    bio: string;
  };
  